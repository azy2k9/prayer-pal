# Use the device timezone to define the prayer day

PrayerPal will use the user's device timezone to determine local midnight, prayer dates, and the evaluation of Prayer Windows. This makes the app follow the user's local day when travelling; a future location-setting decision may refine how times are calculated, but it will not silently reinterpret existing prayer records.
